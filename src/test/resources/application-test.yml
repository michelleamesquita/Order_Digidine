spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/testdb
      authentication-database: testdb # Remove credenciais para testes
  mongodb:
    embedded:
      version: 5.0.0
      features: sync_delay

app:
  base-url: "http://localhost:8080"